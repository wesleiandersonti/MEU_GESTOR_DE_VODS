<Window x:Class="MeuGestorVODs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:MeuGestorVODs"
        Title="{Binding WindowTitle}" Height="700" Width="1100"
        WindowStyle="None"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="StatusBackgroundBrush" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="BaseTextBrush" Color="#111111"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="HeaderForegroundBrush" Color="White"/>
        <SolidColorBrush x:Key="ChromeTabBackground" Color="#DEE1E6"/>
        <SolidColorBrush x:Key="ChromeTabActiveBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="ChromeTabHoverBackground" Color="#E8EAED"/>
        
        <!-- Chrome-style Tab Style -->
        <Style x:Key="ChromeTabItemStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" 
                                Background="{StaticResource ChromeTabBackground}"
                                BorderBrush="#DADCE0"
                                BorderThickness="1"
                                CornerRadius="8 8 0 0"
                                Margin="2,0,0,0"
                                Height="32">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" 
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="12,0,8,0"/>
                                <Button Grid.Column="1" 
                                        x:Name="CloseButton"
                                        Content="×"
                                        Width="16"
                                        Height="16"
                                        Margin="0,0,8,0"
                                        Padding="0"
                                        FontSize="14"
                                        FontWeight="Bold"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="#5F6368"
                                        Click="CloseTab_Click"
                                        ToolTip="Fechar aba"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource ChromeTabActiveBackground}"/>
                                <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource ChromeTabHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Chrome Tab Control Style -->
        <Style x:Key="ChromeTabControlStyle" TargetType="TabControl">
            <Setter Property="TabStripPlacement" Value="Top"/>
            <Setter Property="Background" Value="{StaticResource ChromeTabBackground}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- Chrome Tab Item Style Fixed (sem botão de fechar) -->
        <Style x:Key="ChromeTabItemStyleFixed" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" 
                                Background="{StaticResource ChromeTabBackground}"
                                BorderBrush="#DADCE0"
                                BorderThickness="1"
                                CornerRadius="8 8 0 0"
                                Margin="2,0,0,0"
                                Height="32">
                            <Grid>
                                <ContentPresenter 
                                    ContentSource="Header"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="12,0,12,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource ChromeTabActiveBackground}"/>
                                <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{StaticResource ChromeTabHoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="38"
            ResizeBorderThickness="6"
            CornerRadius="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: Menu + Title + Theme + Window Controls -->
        <Border Grid.Row="0" Background="{DynamicResource ChromeTabBackground}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Height="38">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Menu Button -->
                <Button Grid.Column="0"
                        x:Name="MainMenuButton"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Width="40"
                        Height="30"
                        Margin="8,4,4,4"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="ThemeButton_Click"
                        ToolTip="Menu">
                    <Grid Width="18" Height="14">
                        <Border Height="2" Width="18" CornerRadius="1" Background="#5F6368" VerticalAlignment="Top"/>
                        <Border Height="2" Width="18" CornerRadius="1" Background="#5F6368" VerticalAlignment="Center"/>
                        <Border Height="2" Width="18" CornerRadius="1" Background="#5F6368" VerticalAlignment="Bottom"/>
                    </Grid>
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="1 INICIO" Click="MainMenuHome_Click"/>
                            <MenuItem Header="2 CONECTA XUI-ONE" Click="MainMenuConnectXuiOne_Click"/>
                            <MenuItem Header="3 IP E PORTA" Click="MainMenuIpPort_Click"/>
                            <MenuItem Header="4 YouTube_para_m3u" Click="MainMenuYouTubeToM3u_Click"/>
                            <MenuItem Header="5 DARK M3U CHECKER" Click="MainMenuDarkM3uChecker_Click"/>
                            <MenuItem Header="6 LisoFlix" Click="MainMenuLisoFlix_Click"/>
                            <Separator/>
                            <MenuItem Header="Nova Janela" Click="NewWindow_Click"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                
                <!-- Title -->
                <TextBlock Grid.Column="1" 
                           Text="MEU GESTOR DE VODS" 
                           FontSize="16" 
                           FontWeight="Bold"
                           Foreground="#5F6368"
                           VerticalAlignment="Center"
                           Margin="10,0,20,0"/>
                

                
                <!-- Theme Button -->
                <Button Grid.Column="3"
                        x:Name="ThemeButton"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Content="{Binding ThemeButtonText}"
                        Padding="10,4"
                        Margin="0,0,10,0"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="1"
                        BorderBrush="#DADCE0"
                        Foreground="#5F6368"
                        Click="ThemeButton_Click"
                        ToolTip="Muda tema: Claro, Escuro ou Sistema">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Tema Claro" Click="ThemeLight_Click"/>
                            <MenuItem Header="Tema Escuro" Click="ThemeDark_Click"/>
                            <MenuItem Header="Tema do Sistema" Click="ThemeSystem_Click"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                
                <!-- Window Controls -->
                <Button Grid.Column="4"
                        x:Name="MinimizeWindowButton"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Content="—"
                        Width="45"
                        Height="38"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="14"
                        Foreground="#5F6368"
                        Click="MinimizeWindow_Click"
                        ToolTip="Minimizar"/>
                
                <Button Grid.Column="5"
                        x:Name="MaximizeRestoreWindowButton"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Content="□"
                        Width="45"
                        Height="38"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="14"
                        Foreground="#5F6368"
                        Click="MaximizeRestoreWindow_Click"
                        ToolTip="Maximizar/Restaurar"/>
                
                <Button Grid.Column="6"
                        x:Name="CloseWindowButton"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Content="×"
                        Width="45"
                        Height="38"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#5F6368"
                        Click="CloseWindow_Click"
                        ToolTip="Fechar">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content Area with Tabs -->
        <TabControl Grid.Row="1" 
                    x:Name="ChromeTabControl"
                    Style="{StaticResource ChromeTabControlStyle}"
                    Background="{DynamicResource AppBackgroundBrush}">
            
            <!-- Tab 1: Main Application Content (FIXA - sem botão de fechar) -->
            <TabItem Header="MEU GESTOR DE VODS" Style="{StaticResource ChromeTabItemStyleFixed}">
                <Grid Background="{DynamicResource AppBackgroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Config Panel -->
                    <Grid Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" TextElement.Foreground="{DynamicResource BaseTextBrush}" Margin="10" Grid.IsSharedSizeScope="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- M3U URL -->
                        <Grid Grid.Row="0" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="URL M3u ou M3u8:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <Button Grid.Column="1" Content="Carregar" Click="LoadM3U_Click" Padding="15,5" Margin="0,0,5,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" ToolTip="Carrega e processa a playlist da URL informada"/>
                            <ComboBox Grid.Column="2" x:Name="M3UUrlComboBox" IsEditable="True" Text="{Binding M3UUrl, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                            <Button Grid.Column="3" Content="Histórico" Click="ShowUrlHistory_Click" Padding="12,5" Margin="5,0,0,0" Background="#2196F3" Foreground="White" ToolTip="Mostra o histórico de URLs M3U testadas"/>
                            <Button Grid.Column="4" Content="Limpar Offline" Click="ClearOfflineUrls_Click" Padding="12,5" Margin="5,0,0,0" Background="#F44336" Foreground="White" ToolTip="Configura ou executa a limpeza/verificação de links offline"/>
                            <Button Grid.Column="5" Content="Analisar Link" Click="AnalyzeLinks_Click" Padding="12,5" Margin="5,0,0,0" Background="#3F51B5" Foreground="White" FontWeight="SemiBold" ToolTip="Executa o IPTV Checker (ONLINE/OFFLINE, duplicados e score)"/>
                        </Grid>

                        <!-- Download Path -->
                        <Grid Grid.Row="1" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Pasta Download:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <TextBox Grid.Column="1" Text="{Binding DownloadPath}" Padding="5" IsReadOnly="True"/>
                            <Button Grid.Column="2" Content="Alterar" Click="BrowsePath_Click" Padding="15,5" Margin="10,0,0,0" ToolTip="Escolhe a pasta base de downloads e banco local"/>
                        </Grid>

                        <!-- Local File -->
                        <Grid Grid.Row="2" Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Arquivo Local:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <Button Grid.Column="1" Content="Procurar" Click="BrowseLocalFile_Click" Padding="12,5" Margin="0,0,5,0" Background="#FF9800" Foreground="White" ToolTip="Seleciona uma playlist local no computador"/>
                            <Border Grid.Column="2"
                                    Background="#FAFAFA"
                                    BorderBrush="#BDBDBD"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    AllowDrop="True"
                                    DragEnter="LocalFileDropZone_DragEnter"
                                    DragOver="LocalFileDropZone_DragOver"
                                    DragLeave="LocalFileDropZone_DragLeave"
                                    Drop="LocalFileDropZone_Drop">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#FAFAFA"/>
                                        <Setter Property="BorderBrush" Value="#BDBDBD"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLocalFileDragOver}" Value="True">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                <Setter Property="BorderBrush" Value="#1E88E5"/>
                                                <Setter Property="BorderThickness" Value="2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <ComboBox x:Name="LocalFileComboBox"
                                              Text="{Binding LocalFilePath, UpdateSourceTrigger=PropertyChanged}"
                                              ItemsSource="{Binding LocalFileHistory}"
                                              IsEditable="True"
                                              IsReadOnly="True"
                                              StaysOpenOnEdit="True"
                                              DropDownOpened="LocalFileComboBox_DropDownOpened"
                                              SelectionChanged="LocalFileComboBox_SelectionChanged"
                                              Padding="4"
                                              BorderThickness="0"
                                              Background="Transparent"
                                              AllowDrop="True"
                                              DragEnter="LocalFileDropZone_DragEnter"
                                              DragOver="LocalFileDropZone_DragOver"
                                              DragLeave="LocalFileDropZone_DragLeave"
                                              Drop="LocalFileDropZone_Drop"/>
                                    <TextBlock Text="Arraste e solte sua lista aqui..."
                                               Margin="8,0,0,0"
                                               VerticalAlignment="Center"
                                               Foreground="#5f6368"
                                               FontStyle="Italic"
                                               FontWeight="SemiBold"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding LocalFilePath, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            <Button Grid.Column="3" Content="Analisar lista local" Click="LoadLocalFile_Click" Padding="15,5" Margin="5,0,0,0" Background="#4CAF50" Foreground="White" FontWeight="Bold" ToolTip="Lê e processa a playlist local selecionada"/>
                        </Grid>

                        <!-- Filter -->
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Filtrar:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                            <TextBlock Grid.Column="2" Text="Resultado:" VerticalAlignment="Center" Margin="10,0,6,0" FontWeight="SemiBold"/>
                            <ComboBox Grid.Column="3" Width="140" ItemsSource="{Binding AnalysisFilterOptions}" SelectedItem="{Binding SelectedAnalysisFilter}" Padding="4"/>
                        </Grid>

                        <!-- IPTV Checker Progress -->
                        <Grid Grid.Row="4" Margin="0,6,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Checker:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                            <ProgressBar Grid.Column="1" Height="16" Value="{Binding AnalysisProgressValue}" Maximum="100" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding AnalysisProgressText}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#333"/>
                            <TextBlock Grid.Column="3" Text="{Binding AnalysisSummaryText}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#333" TextTrimming="CharacterEllipsis"/>
                            <Button Grid.Column="4" Content="Remover Duplicados" Click="RemoveDuplicates_Click" Padding="10,4" Margin="8,0,0,0" ToolTip="Remove da lista atual os links marcados como duplicados"/>
                            <Button Grid.Column="5" Content="Exportar Resultado" Click="ExportAnalysis_Click" Padding="10,4" Margin="8,0,0,0" Background="#009688" Foreground="White" ToolTip="Exporta playlist filtrada (ONLINE, sem duplicados ou limpa)"/>
                        </Grid>
                    </Grid>

                    <!-- Main Content -->
                    <Grid Grid.Row="1" Margin="10,5" x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="EntriesColumn" Width="2*"/>
                <ColumnDefinition x:Name="CenterSpacerColumn" Width="10"/>
                <ColumnDefinition x:Name="MonitorColumn" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Entries -->
            <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,6">
                        <Button Content="{Binding ItemCountText}" Click="ItemsSummary_Click" Padding="12,5" Margin="0,0,8,0" FontWeight="SemiBold" ToolTip="Mostra a lista completa de itens carregados"/>
                        <Button Content="{Binding GroupCountText}" Click="GroupsSummary_Click" Padding="12,5" Margin="0,0,8,0" FontWeight="SemiBold" ToolTip="Abre/fecha o painel de grupos por categoria"/>
                        <TextBlock Text="{Binding GroupFilterInfoText}" VerticalAlignment="Center" Foreground="#666"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="EntriesList" ItemsSource="{Binding FilteredEntries}" 
                             AutoGenerateColumns="False" CanUserAddRows="False" 
                             SelectionMode="Extended" GridLinesVisibility="Horizontal"
                             PreviewMouseLeftButtonUp="EntriesList_PreviewMouseLeftButtonUp"
                             EnableRowVirtualization="True"
                             EnableColumnVirtualization="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             MinColumnWidth="70"
                             ScrollViewer.CanContentScroll="True"
                             ScrollViewer.HorizontalScrollBarVisibility="Visible"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CheckStatus}" Value="{x:Static local:ItemStatus.Checking}">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CheckStatus}" Value="{x:Static local:ItemStatus.Ok}">
                                        <Setter Property="Background" Value="#E8F5E9"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding CheckStatus}" Value="{x:Static local:ItemStatus.Error}">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, Mode=TwoWay}" Width="30"/>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" MinWidth="220"/>
                            <DataGridTextColumn Header="Grupo (Categoria | Subcategoria)" Binding="{Binding GroupDisplay}" Width="230"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="90"/>
                            <DataGridTextColumn Header="Servidor" Binding="{Binding ServerHost}" Width="150"/>
                            <DataGridTextColumn Header="Latência (ms)" Binding="{Binding ResponseTimeMs, StringFormat={}{0:F0}}" Width="95"/>
                            <DataGridTextColumn Header="Duplicado" Binding="{Binding DuplicateText}" Width="85"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Margin="10,8,10,0" BorderBrush="#D9D9D9" BorderThickness="1" Padding="8"
                            Visibility="{Binding GroupPanelVisibility}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Grupos encontrados" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TreeView Grid.Row="1" x:Name="GroupsTree" ItemsSource="{Binding GroupCategories}" SelectedItemChanged="GroupsTree_SelectedItemChanged" Height="170"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <TreeView.Resources>
                                    <HierarchicalDataTemplate DataType="{x:Type local:GroupCategoryItem}" ItemsSource="{Binding Groups}">
                                        <TextBlock Text="{Binding CategoryName}" FontWeight="SemiBold"/>
                                    </HierarchicalDataTemplate>
                                    <DataTemplate DataType="{x:Type local:GroupListItem}">
                                        <TextBlock Text="{Binding DisplayName}"/>
                                    </DataTemplate>
                                </TreeView.Resources>
                            </TreeView>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
                        <Button Content="Selecionar Todos" Click="SelectAll_Click" Padding="10,5" Margin="0,0,5,0" ToolTip="Marca todos os itens listados para download"/>
                        <Button Content="Desmarcar Todos" Click="DeselectAll_Click" Padding="10,5" Margin="0,0,20,0" ToolTip="Remove a seleção de todos os itens"/>
                        <Button Content="{Binding DownloadActionButtonText}" Click="DownloadSelected_Click" Padding="15,5" Background="#4CAF50" Foreground="White" ToolTip="Inicia, pausa ou retoma os downloads selecionados"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Panel - Downloads e Score -->
            <Border Grid.Column="2" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F7F7F7" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Monitoramento" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="—" Width="24" Height="20" Margin="0,0,4,0" Click="MonitorMinimize_Click" ToolTip="Minimizar painel"/>
                                <Button Content="□" Width="24" Height="20" Click="MonitorMaximize_Click" ToolTip="Maximizar painel"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <TabControl Grid.Row="1" Margin="6" x:Name="MainMonitorTabs">
                        <TabItem Header="Downloads">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Downloads}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#D7D7D7" BorderThickness="1" CornerRadius="6" Margin="0,0,0,8" Padding="8" Background="White">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="74"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" Width="66" Height="44" CornerRadius="4" Background="#1E1E1E" BorderBrush="#A8A8A8" BorderThickness="1">
                                                        <Grid>
                                                            <Image Source="{Binding LogoUrl}" Stretch="UniformToFill"/>
                                                            <TextBlock Text="{Binding FileType}" Foreground="White" FontWeight="Bold" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,4,2"/>
                                                        </Grid>
                                                    </Border>

                                                    <StackPanel Grid.Column="1" Margin="10,0,8,0">
                                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" FontSize="12"/>
                                                        <ProgressBar Value="{Binding Progress}" Maximum="100" Height="7" Margin="0,6,0,6" Background="#ECECEC" Foreground="#2F8F43"/>
                                                        <WrapPanel>
                                                            <TextBlock Text="{Binding DurationText}" FontSize="10" Foreground="#666" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding DownloadedText}" FontSize="10" Foreground="#666" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding TotalText}" FontSize="10" Foreground="#666" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding SpeedText}" FontSize="10" Foreground="#666" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding StatusIcon}" FontSize="10" Margin="0,0,4,0">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="#4A4A4A"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="downloading">
                                                                                <Setter Property="Foreground" Value="#2F8F43"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="paused">
                                                                                <Setter Property="Foreground" Value="#C77700"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="completed">
                                                                                <Setter Property="Foreground" Value="#2F8F43"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="error">
                                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="canceled">
                                                                                <Setter Property="Foreground" Value="#7A7A7A"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="canceling">
                                                                                <Setter Property="Foreground" Value="#A55B00"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="skipped">
                                                                                <Setter Property="Foreground" Value="#5C6BC0"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                            <TextBlock Text="{Binding Status}" FontSize="10">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="#444"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="downloading">
                                                                                <Setter Property="Foreground" Value="#2F8F43"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="paused">
                                                                                <Setter Property="Foreground" Value="#C77700"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="completed">
                                                                                <Setter Property="Foreground" Value="#2F8F43"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="error">
                                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="canceled">
                                                                                <Setter Property="Foreground" Value="#7A7A7A"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="canceling">
                                                                                <Setter Property="Foreground" Value="#A55B00"/>
                                                                            </DataTrigger>
                                                                            <DataTrigger Binding="{Binding StatusKind}" Value="skipped">
                                                                                <Setter Property="Foreground" Value="#5C6BC0"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </WrapPanel>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="2" VerticalAlignment="Top">
                                                        <Button Content="{Binding}" Width="22" Height="22" Padding="0" Margin="0,0,0,4" Click="ToggleDownloadPause_Click" Tag="{Binding}" ToolTip="{Binding PauseButtonToolTip}" Background="#E8F5E9" BorderBrush="#9CCC65" BorderThickness="1" Foreground="#1B5E20" IsEnabled="{Binding IsActive}">
                                                            <Button.ContentTemplate>
                                                                <DataTemplate>
                                                                    <Grid Width="12" Height="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                        <Path x:Name="PlayIcon" Data="M2,1 L2,11 L10,6 Z" Fill="#1B5E20" Stretch="Fill" Visibility="Collapsed"/>
                                                                        <Grid x:Name="PauseIcon" Width="10" Height="12">
                                                                            <Rectangle Width="3" Height="12" Fill="#1B5E20" HorizontalAlignment="Left"/>
                                                                            <Rectangle Width="3" Height="12" Fill="#1B5E20" HorizontalAlignment="Right"/>
                                                                        </Grid>
                                                                    </Grid>
                                                                    <DataTemplate.Triggers>
                                                                        <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                                                            <Setter TargetName="PlayIcon" Property="Visibility" Value="Visible"/>
                                                                            <Setter TargetName="PauseIcon" Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding IsPaused}" Value="False">
                                                                            <Setter TargetName="PlayIcon" Property="Visibility" Value="Collapsed"/>
                                                                            <Setter TargetName="PauseIcon" Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </DataTemplate.Triggers>
                                                                </DataTemplate>
                                                            </Button.ContentTemplate>
                                                        </Button>
                                                        <Button Content="X" Width="22" Height="22" Padding="0" FontSize="11" FontWeight="Bold" Margin="0,0,0,8" Click="RemoveDownloadItem_Click" Tag="{Binding}" ToolTip="Parar download / remover item" Background="#FFEBEE" BorderBrush="#EF9A9A" BorderThickness="1" Foreground="#B71C1C"/>
                                                        <TextBlock Text="{Binding Progress, StringFormat='{}{0:F1}%'}" FontSize="10" Foreground="#666" HorizontalAlignment="Center"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </TabItem>
                        <TabItem Header="Score Servidores">
                            <DataGrid ItemsSource="{Binding ServerScores}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      GridLinesVisibility="Horizontal"
                                      EnableRowVirtualization="True"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      VirtualizingPanel.VirtualizationMode="Recycling"
                                      MinColumnWidth="55"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Setter Property="Background" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Quality}" Value="{x:Static local:ServerQuality.Excelente}">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Quality}" Value="{x:Static local:ServerQuality.Bom}">
                                                <Setter Property="Background" Value="#F1F8E9"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Quality}" Value="{x:Static local:ServerQuality.Regular}">
                                                <Setter Property="Background" Value="#FFF8E1"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Quality}" Value="{x:Static local:ServerQuality.Ruim}">
                                                <Setter Property="Background" Value="#FFEBEE"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Servidor" Binding="{Binding Host}" Width="*" MinWidth="180"/>
                                    <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat={}{0:F1}}" Width="65"/>
                                    <DataGridTextColumn Header="Qualidade" Binding="{Binding Quality}" Width="85"/>
                                    <DataGridTextColumn Header="Online" Binding="{Binding OnlineLinks}" Width="55"/>
                                    <DataGridTextColumn Header="Offline" Binding="{Binding OfflineLinks}" Width="55"/>
                                    <DataGridTextColumn Header="Taxa %" Binding="{Binding SuccessRate, StringFormat={}{0:F1}}" Width="65"/>
                                    <DataGridTextColumn Header="Resp. ms" Binding="{Binding AverageResponseMs, StringFormat={}{0:F0}}" Width="70"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>
                    
                    <!-- Status Bar -->
                    <Border Grid.Row="2" Background="{DynamicResource StatusBackgroundBrush}" TextElement.Foreground="{DynamicResource BaseTextBrush}" Padding="5,8" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="1" Text="{Binding CurrentVersionText}" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold" FontSize="11">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource BaseTextBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsUpdateAvailable}" Value="True">
                                    <Setter Property="Foreground" Value="#E50914"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <Button Grid.Column="2" Content="Verificar Atualizações" Click="CheckUpdates_Click" Padding="10,4" Margin="0,0,8,0" Background="#FF9800" Foreground="White" FontSize="11" VerticalAlignment="Center" ToolTip="Verifica se existe uma nova versão disponível no GitHub"/>
                <Button Grid.Column="3" Content="Voltar Versão" Click="RollbackVersion_Click" Padding="10,4" Margin="0,0,8,0" Background="#455A64" Foreground="White" FontSize="11" VerticalAlignment="Center" ToolTip="Restaura para uma versão anterior do aplicativo"/>
                <Button Grid.Column="4" Content="Baixar txt VODs" Click="OpenVodLinksTxt_Click" Padding="10,4" Margin="0,0,8,0" FontSize="11" VerticalAlignment="Center" ToolTip="Exporta playlist de VODs em formato compatível com VLC"/>
                <Button Grid.Column="5" Content="Baixar txt Canais" Click="OpenLiveLinksTxt_Click" Padding="10,4" Margin="0,0,8,0" FontSize="11" VerticalAlignment="Center" ToolTip="Exporta playlist de canais em formato compatível com VLC"/>
                <Button Grid.Column="6" Content="Estatísticas BD" Click="ShowDatabaseStats_Click" Padding="10,4" Margin="0,0,8,0" Background="#673AB7" Foreground="White" FontSize="11" VerticalAlignment="Center" ToolTip="Mostra estatísticas detalhadas do banco local SQLite"/>
                <Button Grid.Column="7" Content="GitHub" Click="OpenGitHub_Click" Padding="10,4" FontSize="11" VerticalAlignment="Center" ToolTip="Abre o repositório do projeto no GitHub"/>
            </Grid>
        </Border>

                    <!-- Loading Overlay -->
                    <Border Grid.Row="0" Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Carregando..." Foreground="White" FontSize="16"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
