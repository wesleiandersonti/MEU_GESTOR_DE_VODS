<Window x:Class="MeuGestorVODs.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MeuGestorVODs"
        Title="MEU GESTOR DE VODS v1.2.2" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#007ACC" Padding="15">
            <TextBlock Text="MEU GESTOR DE VODS" FontSize="20" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- Config Panel -->
        <Grid Grid.Row="1" Background="#F5F5F5" Margin="10" Grid.IsSharedSizeScope="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- M3U URL -->
            <Grid Grid.Row="0" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="URL M3U:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Text="{Binding M3UUrl, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
                <Button Grid.Column="2" Content="Carregar" Command="{Binding LoadM3UCommand}" Padding="15,5" Margin="10,0,0,0"/>
            </Grid>

            <!-- Download Path -->
            <Grid Grid.Row="1" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Pasta Download:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Text="{Binding DownloadPath}" Padding="5" IsReadOnly="True"/>
                <Button Grid.Column="2" Content="Alterar" Command="{Binding BrowsePathCommand}" Padding="15,5" Margin="10,0,0,0"/>
            </Grid>

            <!-- Filter -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="Label"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Filtrar:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
            </Grid>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Entries -->
            <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,6">
                        <Button Content="{Binding ItemCountText}" Command="{Binding ItemsSummaryCommand}" Padding="12,5" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <Button Content="{Binding GroupCountText}" Command="{Binding GroupsSummaryCommand}" Padding="12,5" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GroupFilterInfoText}" VerticalAlignment="Center" Foreground="#666"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="EntriesList" ItemsSource="{Binding FilteredEntries}" 
                             AutoGenerateColumns="False" CanUserAddRows="False" 
                             SelectionMode="Extended" GridLinesVisibility="Horizontal"
                             PreviewMouseLeftButtonUp="EntriesList_PreviewMouseLeftButtonUp"
                             EnableRowVirtualization="True"
                             EnableColumnVirtualization="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             ScrollViewer.CanContentScroll="True">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, Mode=TwoWay}" Width="30"/>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Grupo (Categoria | Subcategoria)" Binding="{Binding GroupDisplay}" Width="260"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="2" Margin="10,8,10,0" BorderBrush="#D9D9D9" BorderThickness="1" Padding="8"
                            Visibility="{Binding GroupPanelVisibility}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Grupos encontrados" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TreeView Grid.Row="1" x:Name="GroupsTree" ItemsSource="{Binding GroupCategories}" SelectedItemChanged="GroupsTree_SelectedItemChanged" Height="170">
                                <TreeView.Resources>
                                    <HierarchicalDataTemplate DataType="{x:Type local:GroupCategoryItem}" ItemsSource="{Binding Groups}">
                                        <TextBlock Text="{Binding CategoryName}" FontWeight="SemiBold"/>
                                    </HierarchicalDataTemplate>
                                    <DataTemplate DataType="{x:Type local:GroupListItem}">
                                        <TextBlock Text="{Binding DisplayName}"/>
                                    </DataTemplate>
                                </TreeView.Resources>
                            </TreeView>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10">
                        <Button Content="Selecionar Todos" Command="{Binding SelectAllCommand}" Padding="10,5" Margin="0,0,5,0"/>
                        <Button Content="Desmarcar Todos" Command="{Binding DeselectAllCommand}" Padding="10,5" Margin="0,0,20,0"/>
                        <Button Content="Baixar Selecionados" Command="{Binding DownloadSelectedCommand}" Padding="15,5" Background="#4CAF50" Foreground="White"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Panel - Downloads -->
            <Border Grid.Column="2" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Downloads" FontWeight="Bold" FontSize="14" Margin="10"/>

                    <ScrollViewer Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding Downloads}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                            <ProgressBar Value="{Binding Progress}" Maximum="100" Height="15" Margin="0,5"/>
                                            <Grid>
                                                <TextBlock Text="{Binding Status}" FontSize="11" Foreground="Gray"/>
                                                <TextBlock Text="{Binding Progress, StringFormat='{}{0:F0}%'}" FontSize="11" 
                                                          HorizontalAlignment="Right"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F0F0F0" Padding="10">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Left" Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" CanContentScroll="False">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentVersionText}" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                        <Button Content="Verificar Atualizações" Command="{Binding CheckUpdatesCommand}" Padding="15,5" Margin="0,0,10,0" Background="#FF9800" Foreground="White"/>
                        <Button Content="Voltar Versão" Command="{Binding RollbackVersionCommand}" Padding="15,5" Margin="0,0,10,0" Background="#455A64" Foreground="White"/>
                        <Button Content="Abrir TXT VOD" Command="{Binding OpenVodLinksTxtCommand}" Padding="12,5" Margin="0,0,10,0"/>
                        <Button Content="Abrir TXT Canais" Command="{Binding OpenLiveLinksTxtCommand}" Padding="12,5" Margin="0,0,10,0"/>
                        <Button Content="GitHub" Command="{Binding OpenGitHubCommand}" Padding="15,5"/>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="4" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Carregando..." Foreground="White" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
